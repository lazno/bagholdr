info:
  name: upload csv
  type: http
  seq: 4

http:
  method: post
  url: http://localhost:8080/import/importDirectaCsv
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "csvContent": "<csv data goes here>"
      }
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
